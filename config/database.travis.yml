default: &default
  adapter: mysql2
  encoding: utf8
  username: root
  password:
  host: localhost

development:
  <<: *default
  database: real_estate_agency_development

test:
  <<: *default
  database: real_estate_agency_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
