<p id="notice"><%= notice %></p>

<p>
  <strong><%= City.human_attribute_name(:name) %>:</strong>
  <%= @city.name %>
</p>

<%= link_to t('views.edit'), edit_city_path(@city) %> |
<%= link_to t('views.back'), cities_path %>
